# SEU Compiler 2026 课程实验报告

## 1. 实验目标

本项目实现一个面向课程实践的简化编译器生成链，覆盖：

1. `seuLex`：读取 `.l` 规格，完成 RE 扩展、NFA/DFA 构造、最小化和 scanner 代码生成。
2. `seuYacc`：读取 `.y` 规格，完成 LR(1)/LALR(1) 自动机构造、分析表生成和 parser 代码生成。
3. `intermediate`：接收语法分析阶段构建的 AST，完成符号表管理、三地址码生成和基本块划分。

最终形成一条可运行的最小链路：

`Lex -> Yacc -> AST -> IR`

## 2. 项目结构

根目录核心模块如下：

- `seuLex/`
- `seuYacc/`
- `intermediate/`
- `integration/tests/pipeline/`
- `resources/`
- `docs/`

统一构建入口位于根目录 `CMakeLists.txt`。

## 3. 数据结构复用

项目严格复用中期报告中定义的核心数据结构命名。

### 3.1 Lex

- `node`
- `nfa`
- `dfa`
- `idreTable`
- `nfaTable`
- `dfaterminals`
- `TerStateActionTable`
- `mindfareturn`
- `char_set`

### 3.2 Yacc

- `operators`
- `produce`
- `ITEM`
- `LRnode`
- `LRPDA`
- `parse_table_item`
- `terminals`
- `nonterminals`
- `producers`
- `ops`

### 3.3 Intermediate

- `ASTNodeType`
- `ASTNode`
- `TriOp`
- `TriAddrStmt`
- `IntermediateCode`

## 4. Lex 模块设计

### 4.1 功能实现

`seuLex` 已完成课程要求的 8 项核心能力：

1. Lex 三段解析
2. 扩展 RE 转换
3. 中缀转后缀
4. 单 RE 到 NFA
5. 多 NFA 合并
6. NFA 确定化
7. DFA 最小化
8. 根据最小 DFA 生成 scanner

同时提供 dot 可视化与内建自测。

### 4.2 输出接口

生成的 scanner 支持：

- `analysis(std::string)`
- `next_token()`
- `tokenize(...)`
- `tokenize_detailed(...)`
- `tokenize_for_parser(...)`，当生成时传入 `--token-header`

其中 ABI 模式下，scanner 会直接包含 `seuYacc` 生成的 token 头，并把 `.l` 动作中的 `yylval` 写入 parser `Token.semantic`。

## 5. Yacc 模块设计

### 5.1 功能实现

`seuYacc` 已实现：

1. `.y` 三段解析
2. FIRST 计算
3. LR(1) item 闭包与 GOTO
4. LR(1) PDA 构造
5. LR(1) 分析表构造
6. LALR(1) 状态合并
7. parser 代码生成
8. 生成 token 头与运行时接口

### 5.2 Token ABI

为统一 Lex -> Yacc 联通，本轮收尾补充了稳定导出：

- `SEU_YACC_TOKEN_NAMESPACE`
- `SEU_YACC_TOKEN_TYPE`
- `SEU_YACC_SEMANTIC_TYPE`

这使 `seuLex` 可以在生成时直接对齐 parser 期望的 token 结构，不再依赖手工 semantic 桥接。

## 6. 中间代码模块设计

### 6.1 AST 与符号表

`intermediate` 模块以 AST 为输入，使用 `ASTBuilder` 构造语义节点，使用 `SymbolTable` 维护：

- 全局变量
- 局部变量
- 函数参数
- 作用域层次
- 偏移量

### 6.2 三地址码生成

当前支持的核心 IR 规则包括：

- 变量声明与初始化
- 赋值
- 算术运算
- 函数调用
- 条件跳转
- 循环跳转
- `return`

输出形式是稳定文本三地址码，例如：

```text
1: t1 = 2 * 3
2: t2 = 42 + t1
3: x = t2
4: return x
```

### 6.3 基本块划分

本轮补充了 `splitBasicBlocks(...)` 与 `formatBasicBlocks(...)`：

- leader 规则：
  - 第一条语句
  - 跳转目标语句
  - `goto/if_goto/return` 的后继语句
- 输出稳定的块范围与后继块信息

这使中间代码阶段从“只有三地址码”提升为“具备基本控制流块视图”。

## 7. 三模块联通

当前正式联通方式为：

1. `seuYacc` 生成 `generated_tokens.h`
2. `seuLex` 以 `--token-header generated_tokens.h` 生成 ABI 模式 lexer
3. 直接调用 `tokenize_for_parser(source)`
4. `yyparse(tokens)` 在语义动作中构建 AST
5. 开始符号归约完成后执行 `setParseRoot(...)`
6. 外层驱动 `releaseParseRoot()`
7. `TriAddrGenerator::generate(root)` 输出 IR

集成样例位于：

- `integration/tests/pipeline/test_cases/pipeline_expr.l`
- `integration/tests/pipeline/test_cases/pipeline_expr.y`
- `integration/tests/pipeline/run_pipeline_test.sh`

## 8. 测试与验证

### 8.1 顶层验证

已通过：

```bash
cmake -S . -B build
cmake --build build -j
ctest --test-dir build --output-on-failure
```

### 8.2 模块验证

已通过：

- `seuLex/tests/lex/run_lex_tests.sh`
- `seuYacc/tests/yacc/run_yacc_tests.sh`
- `intermediate/tests/icg/run_icg_tests.sh`

其中 `intermediate` 当前专项测试共 19 条，已覆盖基本块主规则以及稀疏语句号、非法目标两类边界。

## 9. 当前完成度判断

对照课程 PPT 与当前核对表，现阶段可归纳为：

- `seuLex`：完成
- `seuYacc`：完成
- `intermediate`：完成课程主链路版本，并补齐基本块划分
- 三模块最小整链路：完成

仍保留的扩展项：

- 更完整的 C99 语义规则
- LLVM IR / Jimple 输出
- 更大规模的三模块联调样例

## 10. 已知边界

1. 当前 parser 仍以 `bool yyparse(const std::vector<Token>&)` 为主接口。
2. AST 交接仍采用 `setParseRoot/releaseParseRoot`。
3. 生成器默认信任 `.l/.y` 中的用户代码，适用场景是课程实验与可信输入。
4. 当前中间代码以三地址码和基本块视图为主，尚未扩展到 LLVM IR。

## 11. 开发过程总结

本项目的实际推进顺序为：

1. 先完成 Lex 模块
2. 再完成 Yacc 模块
3. 再完成 AST/IR 模块
4. 完成最小整链路联通
5. 收尾阶段统一 Token ABI、补全基本块划分、整理提交材料

这样的顺序保证了每个阶段都存在独立可验证产物，而不是等到最后才一起调试。

## 12. AI 使用记录

本项目使用 AI 作为工程协作工具，主要用于：

- 代码框架生成与重构
- 测试脚本与测试文档补写
- 文档整理
- 最终提交材料归档

所有关键实现、回归测试和仓库内文件变更均经过本地构建与脚本验证。

## 13. 结论

当前仓库已经达到课程项目提交前的完整状态：

- 三个核心模块可独立构建
- 顶层整链路可运行
- 文档、测试、核对表、实验报告和答辩内容稿齐全

如果后续还有时间，优先继续扩展 C 子集覆盖面和后端 IR 输出；但以当前状态提交课程实践已经具备充分支撑。
