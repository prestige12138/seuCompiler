# SEU Compiler 2026 课程实验报告

## 1. 实验目标

本项目围绕课程实践要求，实现一条完整的编译前端与中间代码生成链：

1. `seuLex`：读取 `.l` 规格，完成扩展 RE 处理、NFA/DFA 构造、最小化和 scanner 代码生成。
2. `seuYacc`：读取 `.y` 规格，完成 LR(1)/LALR(1) 自动机构造、分析表生成和 parser 代码生成。
3. `intermediate`：接收语义动作构建的 AST，完成符号表管理、三地址码生成、基本块划分，并进一步输出 LLVM IR 与 Jimple 文本。

最终形成两条正式可验证链路：

- 最小链路：`Lex -> Yacc -> AST -> TAC`
- 完整演示链路：`Lex -> Yacc -> AST -> TAC -> LLVM IR / Jimple`

## 2. 项目结构

根目录核心结构如下：

- `seuLex/`
- `seuYacc/`
- `intermediate/`
- `integration/tests/pipeline/`
- `integration/tests/ir_pipeline/`
- `docs/`
- `resources/`

统一构建入口位于：

- [CMakeLists.txt](/Users/llawliet/代码/seuCompiler/CMakeLists.txt)

## 3. 数据结构复用

项目严格复用中期报告中定义的核心数据结构命名。

### 3.1 Lex

- `node`
- `nfa`
- `dfa`
- `idreTable`
- `nfaTable`
- `dfaterminals`
- `TerStateActionTable`
- `mindfareturn`
- `char_set`

### 3.2 Yacc

- `operators`
- `produce`
- `ITEM`
- `LRnode`
- `LRPDA`
- `parse_table_item`
- `terminals`
- `nonterminals`
- `producers`
- `ops`

### 3.3 Intermediate

- `ASTNodeType`
- `ASTNode`
- `TriOp`
- `TriAddrStmt`
- `IntermediateCode`

## 4. Lex 模块设计

### 4.1 功能实现

`seuLex` 已完成课程要求的核心能力：

1. Lex 三段解析
2. 扩展 RE 转换
3. 中缀 RE 到后缀 RE
4. 单 RE 到 NFA
5. 多 NFA 合并
6. NFA 确定化
7. DFA 最小化
8. 根据最小 DFA 生成 scanner

同时提供：

- dot 可视化
- 自测和独立测试脚本
- 统一 Token ABI 模式

### 4.2 对外接口

生成的 scanner 支持：

- `analysis(std::string)`
- `next_token()`
- `tokenize(...)`
- `tokenize_detailed(...)`
- `tokenize_for_parser(...)`

当生成时传入 `--token-header`，scanner 会直接对齐 `seuYacc` 生成的 token 结构。

## 5. Yacc 模块设计

### 5.1 功能实现

`seuYacc` 已实现：

1. `.y` 三段解析
2. FIRST 计算
3. LR(1) item 闭包与 GOTO
4. LR(1) PDA 构造
5. LR(1) 分析表构造
6. LALR(1) 状态合并
7. parser 代码生成
8. token 头与运行时接口生成

### 5.2 Token ABI

为统一 `Lex -> Yacc` 联通，本项目稳定导出：

- `SEU_YACC_TOKEN_NAMESPACE`
- `SEU_YACC_TOKEN_TYPE`
- `SEU_YACC_SEMANTIC_TYPE`

这样 `seuLex` 可以直接把 `.l` 动作中的 `yylval` 写入 parser 所需的 `Token.semantic`。

## 6. 中间代码模块设计

### 6.1 AST 与符号表

`intermediate` 以 AST 为输入，使用：

- `ASTBuilder` 构造语义节点
- `SymbolTable` 维护全局变量、局部变量、函数参数、作用域层次和偏移量

### 6.2 三地址码生成

当前支持的核心 IR 规则包括：

- 变量声明与初始化
- 赋值
- 算术运算
- 函数调用
- 条件跳转
- 循环跳转
- `return`

输出形式为稳定文本三地址码，例如：

```text
1: t1 = 2 * 3
2: t2 = 42 + t1
3: x = t2
4: return x
```

### 6.3 基本块划分

项目实现了：

- `splitBasicBlocks(...)`
- `formatBasicBlocks(...)`

leader 规则包括：

- 第一条语句
- 跳转目标语句
- `goto / if_goto / return` 的后继语句

### 6.4 LLVM IR 与 Jimple 输出

本轮最终收尾补齐了基于 AST + 三地址码的目标 IR 文本输出：

- `formatLlvmIr(root, code, options)`
- `formatJimple(root, code, options)`

设计原则：

- 不扩展报告规定的 `TriAddrStmt` / `IntermediateCode`
- 基于 AST 恢复函数签名、参数和局部变量
- 基于三地址码恢复控制流与临时变量
- LLVM IR 采用非 SSA 的 `alloca/load/store` 风格
- Jimple 采用方法、local、label 的稳定文本形式

当前完整演示样例如下：

- 输入：
  - [demo_ir.l](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.l)
  - [demo_ir.y](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.y)
  - [demo_ir.c](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.c)
- 输出：
  - [demo_ir.tac](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/expected/demo_ir.tac)
  - [demo_ir.ll](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/expected/demo_ir.ll)
  - [demo_ir.jimple](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/expected/demo_ir.jimple)

## 7. 三模块联通

当前正式联通方式为：

1. `seuYacc` 现场生成 `generated_tokens.h`
2. `seuLex` 以 `--token-header generated_tokens.h` 生成 ABI 模式 lexer
3. 调用 `tokenize_for_parser(source)`
4. `yyparse(tokens)` 在语义动作中构建 AST
5. 开始符号归约完成后执行 `setParseRoot(...)`
6. 外层驱动 `releaseParseRoot()`
7. `TriAddrGenerator::generate(root)` 输出 TAC
8. `formatLlvmIr(...)` / `formatJimple(...)` 输出目标 IR

正式集成样例包括：

- 最小链路：
  - [integration/tests/pipeline/test_cases/pipeline_expr.l](/Users/llawliet/代码/seuCompiler/integration/tests/pipeline/test_cases/pipeline_expr.l)
  - [integration/tests/pipeline/test_cases/pipeline_expr.y](/Users/llawliet/代码/seuCompiler/integration/tests/pipeline/test_cases/pipeline_expr.y)
- 完整 IR 演示：
  - [integration/tests/ir_pipeline/test_cases/demo_ir.l](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.l)
  - [integration/tests/ir_pipeline/test_cases/demo_ir.y](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.y)
  - [integration/tests/ir_pipeline/test_cases/demo_ir.c](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/test_cases/demo_ir.c)

## 8. 测试与验证

### 8.1 顶层验证

已通过：

```bash
cmake -S . -B build
cmake --build build -j
ctest --test-dir build --output-on-failure
```

最新顶层结果：

- `5/5` 测试通过

### 8.2 模块验证

已通过：

- `seuLex/tests/lex/run_lex_tests.sh`
- `seuYacc/tests/yacc/run_yacc_tests.sh`
- `intermediate/tests/icg/run_icg_tests.sh`

其中：

- `intermediate` 当前专项测试 `26/26` 通过，覆盖：
  - AST 构造
  - 符号表
  - TAC 生成
  - 基本块划分
  - LLVM IR 输出
  - Jimple 输出
  - invalid-target fallback

### 8.3 集成验证

已通过：

- [integration/tests/pipeline/PIPELINE_TEST_REPORT.md](/Users/llawliet/代码/seuCompiler/integration/tests/pipeline/PIPELINE_TEST_REPORT.md)
- [integration/tests/ir_pipeline/IR_PIPELINE_TEST_REPORT.md](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/IR_PIPELINE_TEST_REPORT.md)

最新联调归档材料：

- 顶层回归截图：[ctest_snapshot_20260415.svg](/Users/llawliet/代码/seuCompiler/docs/assets/final_submission/ctest_snapshot_20260415.svg)
- 最小链路截图：[pipeline_snapshot_20260415.svg](/Users/llawliet/代码/seuCompiler/docs/assets/final_submission/pipeline_snapshot_20260415.svg)
- 完整 IR 链路截图：[ir_pipeline_snapshot_20260415.svg](/Users/llawliet/代码/seuCompiler/docs/assets/final_submission/ir_pipeline_snapshot_20260415.svg)
- 最小链路结果目录：[results/20260415_122316](/Users/llawliet/代码/seuCompiler/integration/tests/pipeline/results/20260415_122316)
- 完整 IR 结果目录：[results/20260415_122323](/Users/llawliet/代码/seuCompiler/integration/tests/ir_pipeline/results/20260415_122323)

## 9. 最终提交材料归档

本轮已把课程提交所需的关键材料进一步集中归档到：

- [实践PPT核对表.md](/Users/llawliet/代码/seuCompiler/实践PPT核对表.md)
- [docs/FINAL_SUBMISSION.md](/Users/llawliet/代码/seuCompiler/docs/FINAL_SUBMISSION.md)
- [docs/FINAL_AUDIT.md](/Users/llawliet/代码/seuCompiler/docs/FINAL_AUDIT.md)

其中重点新增了：

- `demo_ir` 的 `.l/.y/.c` 输入和 `TAC/LLVM IR/Jimple` 输出入口
- 三模块联调截图附件
- 最新顶层、模块级和集成级测试报告入口

本轮未处理的唯一人工项是答辩 `ppt/pptx/pdf` 成品文件；仓库内仅保留答辩内容稿，供后续人工转制。

## 10. 期末提交材料归档

按 PPT 第 34 页，当前仓库已准备好以下正式材料：

- `Lex` / `Yacc` 输入文件
- 词法、语法分析程序生成器源程序
- 中间代码生成模块源程序
- 生成的词法、语法分析程序源程序
- 词法、语法分析测试用例与测试报告
- 生成的中间代码样例
- 实验报告与答辩内容稿

完整核对见：

- [实践PPT核对表.md](/Users/llawliet/代码/seuCompiler/实践PPT核对表.md)
- [docs/FINAL_SUBMISSION.md](/Users/llawliet/代码/seuCompiler/docs/FINAL_SUBMISSION.md)

## 11. 已知边界

1. `parser` 仍以 `bool yyparse(const std::vector<Token>&)` 为主接口。
2. AST 交接仍采用 `setParseRoot/releaseParseRoot`。
3. `.l/.y` 中的用户动作和嵌入代码默认按“可信输入”处理。
4. 当前 LLVM IR / Jimple 为课程演示级稳定文本，不涉及 SSA、`phi`、指针、数组和结构体 lowering。

## 12. 开发过程总结

本项目的推进顺序为：

1. 先完成 Lex 模块
2. 再完成 Yacc 模块
3. 再完成 AST/TAC 模块
4. 完成最小整链路联通
5. 收尾阶段统一 Token ABI
6. 补齐基本块、LLVM IR、Jimple 输出
7. 整理测试、文档、核对表和提交材料

这种顺序保证每个阶段都有独立可验证产物，而不是等到最后才一起调试。

## 13. AI 使用记录

本项目使用 AI 作为工程协作工具，主要用于：

- 模块化重构
- 文档补写与测试报告整理
- LLVM IR / Jimple 输出扩展
- 联调脚本与最终提交材料核对
- 多轮构建、回归、审查与修复闭环

本轮明确使用的协作角色包括：

- `explorer`
- `architect`
- `tdd-guide`
- `code-reviewer`
- `security-reviewer`
- `loop-operator`

约束说明：

- 所有关键实现均以本地构建和脚本回归为准
- AI 生成内容均已落到仓库文件并被实际命令验证
- 最终提交材料索引与报告章节由人工确认课程要求后整理

## 14. 结论

按当前收尾范围，当前仓库除答辩 PPT 成品外，已经达到课程项目最终提交状态：

- 三个核心模块可独立构建与测试
- 顶层整链路可统一构建与测试
- 最小链路和完整 IR 演示链路均已打通
- 文档、测试报告、实验报告、答辩内容稿和期末核对表齐全

如果后续仍有时间，优先继续扩展更完整的 C 子集语义和真实后端工具链校验；但以当前状态提交课程实践，已经具备充分支撑。
